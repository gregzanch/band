import { RayTracer } from "@/solvers";

export type RayTracerSolverPropertiesProps = {
  raytracer: RayTracer;
};

export function RayTracerSolverProperties({ raytracer }: RayTracerSolverPropertiesProps) {
  return <>sdf</>;
}
