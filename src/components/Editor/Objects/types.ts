export enum ObjectType {
  SOURCE = "Source",
  RECEIVER = "Receiver",
  MESH = "Mesh",
  GROUP = "Group",
}

export enum LayerMap {
  OBJECTS = 1,
  ENVIRONMENT = 2,
  LIGHTS = 3,
  TRANSFORM_CONTROLS = 4,
}
